# AIndia Recursion Harness

Verified from this Mac: 2026-06-12 IST

## Rule

Perfection is a loop, not a claim.

AIndia should not say it is perfect, hallucination-free, or sovereign by slogan. It should observe live state, simulate options, gate weak routes, absorb verified rails, apply one bounded change, and verify the result.

## Mac Absorbed

Live body truth:

- MacBook Pro is the canonical builder/runtime body.
- M4 mini is the control plane.
- Phones are limbs, not sovereign runtimes.
- Ollama is live on `127.0.0.1:11434`.
- Local models installed: Sarvam translate GGUF, embeddinggemma, ministral, qwen3-coder, mirrorstudent, phi4-mini.
- Codex and Claude cognitive adapters are healthy on `8926` and `8927`.
- `phone-mesh` listens on `8875`, but current Pixel and OnePlus backends are unreachable.
- The previous `localhost:3457/aindia` target is stale until this worktree server is restarted.

## Absorbed Decision

The winning path is:

1. Public wedge: AIndia Check PWA.
2. Runtime proof: Mac local harness with Ollama, Sarvam, claim guard, deterministic gates, receipts.
3. Doctrine: Sanatana Tech stays as internal civilizational spine and R&D lab, not the public proof claim.
4. Product moat: Active Mirror owns the deterministic wrapper/harness layer.
5. Phone path: GrapheneOS-safe phone-mesh remains optional until live backend health passes.
6. Android AICore remains a supported-device candidate for future native wrappers, not this body's canonical phone inference path.

## API Surface

Implemented:

- `/api/aindia/recursion`
- Included in `/api/aindia/sovereignty`
- Included in `/api/aindia/contracts`
- Cached by the AIndia PWA shell for offline reference
- Full 100-pass deterministic recursion ledger exposed under `hundredRecursions.ledger`

## 100 Recursions

The runtime now generates 100 deterministic recursions from:

- scenario pressure
- trust/latency/sovereignty/determinism/cheap-device/proof scores
- fail-closed gate result
- absorbed/backlog/blocked status
- local receipt id

The page shows the summary and a compact first/final ledger window. The API exposes the full ledger.

## Forever Loop

Created automation:

- id: `aindia-recursion-loop`
- cadence: every 6 hours
- workspace: `/Users/mirror-pro/.codex/worktrees/activemirror-genui-governed`
- rule: one bounded improvement per wake, then verify and stop

This is not an uncontrolled autonomous daemon. It is a governed improvement cadence:

- no deploys
- no Cloudflare/Hetzner/runtime topology mutation
- no destructive git
- no broad model training
- no production restart
- no more than one safe local slice per run

## Pass/Fail Standard

A route can be public only if it has:

- live source or local receipt
- explicit consent path
- low-latency fallback
- cheap-device fallback
- deterministic gate result
- clear block state when the rail is unavailable

Anything else stays a hypothesis.
